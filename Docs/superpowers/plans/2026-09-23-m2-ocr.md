# M2 OCR (Core-side) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sidecar Python chạy PP-OCR thật (`ocr.spot`), Core có diff session-seen + CLI `ocr` + serve method `ocr.spot`, và ghi số đo thực vào `Docs/performance.md`.

**Architecture:** Core-side trước (không App UI, không notification — milestone sau). Sidecar: `OcrEngine` Protocol + `PaddleOcrEngine` (lazy init, `lang="en"`), nhận PNG base64 qua JSON-RPC stdio. Core: `OcrPipeline` (timeout 30s) → `OcrDiff` (session-seen set) → thêm `new_texts` vào result; dùng chung handler cho CLI one-shot và serve mode.

**Tech Stack:** Python 3.13 (uv, `paddlepaddle==3.3.1` + `paddleocr` + `pillow` — đã verify resolve trên máy này), C# .NET 10 (`net10.0-windows` Core, TFM test `net10.0-windows10.0.19041.0`), xUnit v2.

## Global Constraints

- Spec `Docs/superpowers/specs/2026-09-23-m2-ocr-design.md` là nguồn yêu cầu; plan này resolve 2 điểm mơ hồ của spec (ghi ở cuối phần này) — các giá trị dưới đây là exact, dùng verbatim.
- **Method split (resolve):** sidecar `ocr.spot` result = `{ spots: [{box, text, score}], elapsed_ms }` (KHÔNG có `new_texts` — sidecar không có diff). Core serve `ocr.spot` và stdout CLI = result sidecar **+ `new_texts: string[]`** (spec §3.1/§3.4 gộp cả hai dưới một tên; tách như spec §3 phân quyền `core.*`/sidecar).
- `box` = tứ giác 4 điểm `[[x,y],[x,y],[x,y],[x,y]]` (float); `score` float; `elapsed_ms` int.
- Error codes: sidecar `-32602` (thiếu/invalid image — exception `RpcInvalidParams`), `-32603` (các lỗi khác — nhánh mặc định của `handle_line`). Core serve: missing/empty image → `-32602` (`RpcMethodException`), sidecar không sẵn sàng/lỗi khác → `-32603`.
- Timeout: `OcrPipeline.Timeout = 30s` (bao gồm cả lần init model đầu).
- Transport: PNG base64 trong JSON, **không** prefix `data:`.
- `PaddleOcrEngine(lang="en")`, lazy init — import `paddleocr`/load model ở **first `spot` call**, không ở import module, không ở serve start.
- Diff: session-seen set, `StringComparison.OrdinalIgnoreCase`, bỏ qua text rỗng/whitespace; set không persist.
- Test skip êm style `SidecarProcessTests` (early return, **không** `Assert.Skip`): thiếu venv/paddle/model → return, **không bao giờ fail vì môi trường**. Test C#: không `Console.WriteLine` (dùng `ITestOutputHelper` nếu cần log); Python: Google docstring.
- C# public API: doc comment `///` song ngữ (câu tiếng Anh trước, tiếng Việt ngay sau) — theo AGENTS.md.
- Python mọi lệnh chạy qua `uv` từ `ReTran.OCR/` (không dùng system Python). Test C#: `dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0` — không đổi TFM, không đổi package version (xUnit v2, bUnit 2.x).
- **Không env-hook product** (vd. `RETRAN_OCR_FAKE`) — test injection qua design đã chốt: `make_spot_handler(engine)` (Python) và interface `ISidecarRpc` (C#).
- Một commit mỗi task. Message: Python/feature `feat(ocr): ...` / test `test(ocr): ...` / fix `fix(ocr): ...` / docs `docs(perf): ...`.
- Số liệu benchmark phải **đo thật** trên máy này, không ước lượng.

---

## File Structure

| Hành động | File | Trách nhiệm |
|---|---|---|
| Create | `ReTran.OCR/src/retran_core/ocr/__init__.py` | package marker |
| Create | `ReTran.OCR/src/retran_core/ocr/engine.py` | `Spot` + `OcrEngine` Protocol |
| Create | `ReTran.OCR/src/retran_core/ocr/image.py` | `decode_png_b64` → numpy BGR |
| Create | `ReTran.OCR/src/retran_core/ocr/handlers.py` | `make_spot_handler` (contract `ocr.spot`) |
| Create | `ReTran.OCR/src/retran_core/ocr/paddle_engine.py` | `PaddleOcrEngine` lazy |
| Modify | `ReTran.OCR/src/retran_core/jsonrpc.py` | + `RpcInvalidParams` → `-32602` |
| Modify | `ReTran.OCR/src/retran_core/__main__.py` | wire `ocr.spot` |
| Modify | `ReTran.OCR/pyproject.toml` | deps: pillow, paddlepaddle, paddleocr |
| Create | `ReTran.OCR/tests/test_ocr_handlers.py` | unit handler/decode/errors (FakeEngine) |
| Create | `ReTran.OCR/tests/test_paddle_integration.py` | integration thật (skip nếu thiếu paddle) |
| Create | `ReTran.Core/Ocr/OcrDiff.cs` | session-seen diff |
| Create | `ReTran.Core/Ocr/OcrPipeline.cs` | call sidecar + timeout + `new_texts` |
| Create | `ReTran.Core/Ocr/OcrBootstrap.cs` | find repo root + resolve pythonExe |
| Create | `ReTran.Core/OcrClient/ISidecarRpc.cs` | abstraction CallAsync |
| Create | `ReTran.Core/OcrClient/SidecarRpcException.cs` | lỗi sidecar trả về (code + message) |
| Create | `ReTran.Core/Cli/OcrCommands.cs` | subcommand `ocr` |
| Create | `ReTran.Core/JsonRpc/RpcMethodException.cs` | exception map đúng error code |
| Modify | `ReTran.Core/OcrClient/SidecarProcess.cs` | implement `ISidecarRpc` + route error/EOF + drain stderr |
| Modify | `ReTran.Core/JsonRpc/JsonRpcServer.cs` | catch `RpcMethodException` |
| Modify | `ReTran.Core/Program.cs` | đăng ký `ocr` CLI + serve spawn sidecar + method `ocr.spot` |
| Create | `ReTran.Tests/OcrDiffTests.cs` | diff unit |
| Create | `ReTran.Tests/OcrPipelineTests.cs` | pipeline + fake `ISidecarRpc` + integration sidecar thật |
| Create | `ReTran.Tests/OcrCliTests.cs` | CLI dispatch + missing file + integration gated |
| Create | `ReTran.Tests/ServeOcrTests.cs` | serve `ocr.spot` qua FakeTransport |
| Modify | `ReTran.Tests/SidecarProcessTests.cs` | + test sidecar error routing (`-32602`) |
| Create | `Docs/performance.md` | số đo thật |

---

### Task 1: Sidecar contract — `ocr.spot` + decode + engine Protocol (không paddle)

**Files:**
- Create: `ReTran.OCR/src/retran_core/ocr/{__init__.py,engine.py,image.py,handlers.py}`
- Create: `ReTran.OCR/tests/test_ocr_handlers.py`
- Modify: `ReTran.OCR/src/retran_core/jsonrpc.py` (thêm `RpcInvalidParams`)
- Modify: `ReTran.OCR/pyproject.toml` (thêm `pillow` qua `uv add`)

**Interfaces:**
- Consumes: `handle_line`/`Handler` hiện hữu (`retran_core/jsonrpc.py`).
- Produces (Task 2+ dùng):
  - `Spot(box: list[list[float]], text: str, score: float)` — NamedTuple, `.to_dict()`
  - `OcrEngine` Protocol: `spot(image_bgr: npt.NDArray[np.uint8]) -> list[Spot]`
  - `decode_png_b64(image_b64: str) -> npt.NDArray[np.uint8]` (BGR, HWC) — raise `ValueError` khi hỏng
  - `make_spot_handler(engine: OcrEngine) -> Handler` — params `{image}`, result `{spots, elapsed_ms}`; raise `RpcInvalidParams` khi image thiếu/hỏng
  - `RpcInvalidParams` (từ `retran_core.jsonrpc`) → `-32602`
  - **Lưu ý:** Task 1 CHƯA wire vào `__main__` (Task 2 wire cùng `PaddleOcrEngine`) — `ocr.spot` chưa gọi được qua serve cho tới khi Task 2 xong.

- [ ] **Step 1: Thêm pillow**

```powershell
# workdir: ReTran.OCR/
uv add pillow
# Expected: pillow==12.3.0 resolved, uv.lock updated
```

- [ ] **Step 2: Viết failing tests**

Create `ReTran.OCR/tests/test_ocr_handlers.py`:

```python
"""Unit tests for the ocr.spot handler contract (FakeEngine, no paddle)."""
from __future__ import annotations

import base64
import io
import json

import numpy as np
import pytest
from PIL import Image

from retran_core.jsonrpc import RpcInvalidParams, handle_line
from retran_core.ocr.engine import Spot
from retran_core.ocr.handlers import make_spot_handler
from retran_core.ocr.image import decode_png_b64


def _png_b64(w: int = 64, h: int = 32, color: tuple[int, int, int] = (255, 255, 255)) -> str:
    """Encode a solid-color RGB PNG as base64 ASCII."""
    buf = io.BytesIO()
    Image.new("RGB", (w, h), color).save(buf, format="PNG")
    return base64.b64encode(buf.getvalue()).decode("ascii")


class FakeEngine:
    """Deterministic engine: always returns one fixed spot."""

    def spot(self, image_bgr: np.ndarray) -> list[Spot]:
        assert image_bgr.ndim == 3 and image_bgr.shape[2] == 3
        return [
            Spot(
                box=[[0.0, 0.0], [10.0, 0.0], [10.0, 5.0], [0.0, 5.0]],
                text="HELLO",
                score=0.99,
            )
        ]


def test_decode_png_b64_returns_bgr_uint8():
    arr = decode_png_b64(_png_b64(color=(255, 0, 0)))  # pure red RGB
    assert arr.dtype == np.uint8
    assert arr.shape == (32, 64, 3)
    assert int(arr[0, 0, 2]) == 255  # red is channel 2 in BGR
    assert int(arr[0, 0, 0]) == 0


def test_decode_png_b64_invalid_raises_value_error():
    with pytest.raises(ValueError):
        decode_png_b64("not-valid-base64!!")


def test_spot_handler_returns_spots_and_elapsed():
    handler = make_spot_handler(FakeEngine())
    result = handler({"image": _png_b64()})
    assert result["spots"][0]["text"] == "HELLO"
    assert result["spots"][0]["box"] == [[0.0, 0.0], [10.0, 0.0], [10.0, 5.0], [0.0, 5.0]]
    assert result["spots"][0]["score"] == pytest.approx(0.99)
    assert isinstance(result["elapsed_ms"], int)
    assert result["elapsed_ms"] >= 0
    assert "new_texts" not in result  # Core adds that, not the sidecar


def test_spot_missing_image_raises_invalid_params():
    handler = make_spot_handler(FakeEngine())
    with pytest.raises(RpcInvalidParams):
        handler({})
    with pytest.raises(RpcInvalidParams):
        handler({"image": ""})


def test_handle_line_maps_invalid_image_to_32602():
    handlers = {"ocr.spot": make_spot_handler(FakeEngine())}
    req = json.dumps({"jsonrpc": "2.0", "id": "1", "method": "ocr.spot", "params": {"image": "@@@"}})
    resp = json.loads(handle_line(req, handlers))
    assert resp["error"]["code"] == -32602


def test_handle_line_happy_path_returns_result():
    handlers = {"ocr.spot": make_spot_handler(FakeEngine())}
    req = json.dumps({"jsonrpc": "2.0", "id": "7", "method": "ocr.spot", "params": {"image": _png_b64()}})
    resp = json.loads(handle_line(req, handlers))
    assert resp["id"] == "7"
    assert resp["result"]["spots"][0]["text"] == "HELLO"
```

- [ ] **Step 3: Chạy test — mong FAIL (import error)**

```powershell
# workdir: ReTran.OCR/
uv run pytest tests/test_ocr_handlers.py -v
# Expected: FAIL — ModuleNotFoundError: No module named 'retran_core.ocr'
```

- [ ] **Step 4: Implement**

`ReTran.OCR/src/retran_core/ocr/__init__.py`:

```python
"""OCR package: engine contract, image decode, and the ocr.spot handler."""
```

`ReTran.OCR/src/retran_core/ocr/engine.py`:

```python
"""OCR engine contract shared by fake and real implementations."""
from __future__ import annotations

from typing import NamedTuple, Protocol

import numpy as np
import numpy.typing as npt


class Spot(NamedTuple):
    """One detected text region.

    Attributes:
        box: Quadrilateral corners [[x, y], [x, y], [x, y], [x, y]] in pixels.
        text: Recognized text.
        score: Recognition confidence in [0, 1].
    """

    box: list[list[float]]
    text: str
    score: float

    def to_dict(self) -> dict:
        """Serialize to the ocr.spot JSON shape (no rounding)."""
        return {"box": self.box, "text": self.text, "score": self.score}


class OcrEngine(Protocol):
    """Detects and recognizes text in a BGR image."""

    def spot(self, image_bgr: npt.NDArray[np.uint8]) -> list[Spot]:
        """Return detected text regions for one image (HWC uint8 BGR)."""
        ...
```

`ReTran.OCR/src/retran_core/ocr/image.py`:

```python
"""PNG base64 decode to the BGR array paddle expects."""
from __future__ import annotations

import base64
import io

import numpy as np
import numpy.typing as npt
from PIL import Image


def decode_png_b64(image_b64: str) -> npt.NDArray[np.uint8]:
    """Decode a base64 PNG string to an HWC uint8 BGR array.

    Args:
        image_b64: Base64-encoded PNG bytes, no data-url prefix.

    Returns:
        Image pixels in BGR channel order (paddle/OpenCV convention).

    Raises:
        ValueError: The input is not valid base64 or not a decodable PNG.
    """
    try:
        raw = base64.b64decode(image_b64, validate=True)
        rgb = Image.open(io.BytesIO(raw)).convert("RGB")
    except Exception as exc:
        raise ValueError(f"invalid PNG image: {exc}") from exc
    return np.asarray(rgb, dtype=np.uint8)[:, :, ::-1].copy()
```

`ReTran.OCR/src/retran_core/ocr/handlers.py`:

```python
"""JSON-RPC handler factory for ocr.spot (engine injected for testability)."""
from __future__ import annotations

import time
from typing import Any, Callable, Optional

from ..jsonrpc import RpcInvalidParams
from .engine import OcrEngine
from .image import decode_png_b64

Handler = Callable[[Optional[dict]], Any]


def make_spot_handler(engine: OcrEngine) -> Handler:
    """Build the ocr.spot handler bound to an engine.

    Args:
        engine: Engine used for every call; not owned by the handler.

    Returns:
        A JSON-RPC handler: params ``{image}`` -> ``{spots, elapsed_ms}``;
        raises RpcInvalidParams for missing/undecodable images.
    """

    def spot(params: Optional[dict]) -> dict:
        if not isinstance(params, dict) or not isinstance(params.get("image"), str) or not params["image"]:
            raise RpcInvalidParams("missing or invalid 'image' (non-empty base64 PNG string)")
        t0 = time.perf_counter()
        try:
            image_bgr = decode_png_b64(params["image"])
        except ValueError as exc:
            raise RpcInvalidParams(str(exc)) from exc
        spots = engine.spot(image_bgr)
        elapsed_ms = int((time.perf_counter() - t0) * 1000)
        return {"spots": [s.to_dict() for s in spots], "elapsed_ms": elapsed_ms}

    return spot
```

`ReTran.OCR/src/retran_core/jsonrpc.py` — thêm NGAY TRƯỚC `def handle_line` và sửa nhánh `except` trong `handle_line`:

```python
class RpcInvalidParams(Exception):
    """Handler error mapped to JSON-RPC -32602 (invalid params).

    Attributes:
        message: Human-readable reason returned to the caller.
    """
```

Trong `handle_line`, thay khối `try/except` hiện có bằng:

```python
    try:
        return _resp(id_, handler(params))
    except RpcInvalidParams as exc:
        return _err(id_, -32602, str(exc))
    except Exception as exc:  # noqa: BLE001 - report any handler failure as internal error
        return _err(id_, -32603, str(exc))
```

- [ ] **Step 5: Chạy test — mong PASS**

```powershell
# workdir: ReTran.OCR/
uv run pytest tests/ -v
# Expected: PASS — 6 test mới + 3 test jsonrpc cũ = 9 passed
```

- [ ] **Step 6: Commit**

```powershell
git add "ReTran.OCR/src/retran_core/ocr/" "ReTran.OCR/src/retran_core/jsonrpc.py" "ReTran.OCR/tests/test_ocr_handlers.py" "ReTran.OCR/pyproject.toml" "uv.lock" "ReTran.OCR/uv.lock"
# (uv.lock path: chỉ add file lock nào thực sự thay đổi — chạy `git status` để xác nhận)
git commit -m "feat(ocr): sidecar ocr.spot contract (PNG decode, engine protocol)"
```

---

### Task 2: `PaddleOcrEngine` thật + wire `__main__` + integration test

**Files:**
- Create: `ReTran.OCR/src/retran_core/ocr/paddle_engine.py`
- Create: `ReTran.OCR/tests/test_paddle_integration.py`
- Modify: `ReTran.OCR/src/retran_core/__main__.py` (wire `ocr.spot`)
- Modify: `ReTran.OCR/pyproject.toml` (thêm `paddlepaddle`, `paddleocr`)

**Interfaces:**
- Consumes (Task 1): `Spot`, `OcrEngine`, `decode_png_b64`, `make_spot_handler`, handler contract `{spots, elapsed_ms}`.
- Produces: `PaddleOcrEngine(lang: str = "en")` — lazy; sidecar serve có method `ocr.spot` thật (Task 4/5 test integration qua sidecar đã wire).

- [ ] **Step 1: Thêm deps — verify resolve (đã controller pre-check OK: `paddlepaddle==3.3.1`)**

```powershell
# workdir: ReTran.OCR/
uv add paddlepaddle paddleocr
# Expected: resolve thành công (controller đã dry-run trước; nếu FAIL => BLOCKED, báo lại controller)
```

- [ ] **Step 2: Probe API của paddleocr đã cài (không code, chỉ observe)**

```powershell
# workdir: ReTran.OCR/
uv run python -c "import paddleocr, inspect; print('paddleocr', paddleocr.__version__); from paddleocr import PaddleOCR; print('has predict:', hasattr(PaddleOCR, 'predict')); print('has ocr:', hasattr(PaddleOCR, 'ocr'))"
# Expected: in version + True/False cho predict/ocr — dùng làm căn cứ Step 4 parser (3.x: predict; 2.x: ocr)
```

Ghi kết quả vào report (version + has predict/ocr).

- [ ] **Step 3: Viết failing integration test**

Create `ReTran.OCR/tests/test_paddle_integration.py`:

```python
"""Integration test against the real PaddleOCR engine (skips when paddle is absent)."""
from __future__ import annotations

import base64
import importlib.util
import io

import pytest
from PIL import Image, ImageDraw

pytestmark = pytest.mark.skipif(
    importlib.util.find_spec("paddleocr") is None,
    reason="paddleocr not installed (uv sync) -> skip",
)


def _text_png_b64() -> str:
    """Render 'HELLO RETRAN' on white and return base64 PNG."""
    img = Image.new("RGB", (640, 96), "white")
    d = ImageDraw.Draw(img)
    d.text((20, 24), "HELLO RETRAN", fill="black")
    buf = io.BytesIO()
    img.save(buf, format="PNG")
    return base64.b64encode(buf.getvalue()).decode("ascii")


def test_paddle_spot_finds_hello():
    from retran_core.ocr.image import decode_png_b64
    from retran_core.ocr.paddle_engine import PaddleOcrEngine

    engine = PaddleOcrEngine(lang="en")
    spots = engine.spot(decode_png_b64(_text_png_b64()))
    assert len(spots) > 0
    joined = " ".join(s.text.upper() for s in spots)
    assert "HELLO" in joined  # Rec model may split lines; require the word somewhere
    for s in spots:
        assert len(s.box) == 4
        assert 0.0 <= s.score <= 1.0


def test_paddle_engine_is_lazy():
    """Module import must not pull paddleocr (stdlib-fast sidecar startup)."""
    import retran_core.ocr.paddle_engine as mod

    src = open(mod.__file__, encoding="utf-8").read()
    assert "import paddleocr" not in src.split("def ")[0]  # no top-level paddleocr import
```

- [ ] **Step 4: Implement `PaddleOcrEngine`**

Create `ReTran.OCR/src/retran_core/ocr/paddle_engine.py`:

```python
"""Real PP-OCR engine: lazy PaddleOCR wrapper (model loads on first spot call)."""
from __future__ import annotations

from typing import Any

import numpy as np
import numpy.typing as npt

from .engine import Spot


class PaddleOcrEngine:
    """Text spotting via PaddleOCR; lang passed through to PaddleOCR(lang=...).

    The paddleocr import and model load happen inside the first spot() call so
    the sidecar stays stdlib-fast to start and reports a clean -32603 when the
    optional deps are missing.
    """

    def __init__(self, lang: str = "en") -> None:
        self.lang = lang
        self._ocr: Any = None

    def spot(self, image_bgr: npt.NDArray[np.uint8]) -> list[Spot]:
        """Detect + recognize text in one BGR image.

        Args:
            image_bgr: HWC uint8 image in BGR order.

        Returns:
            Detected spots in engine order.

        Raises:
            RuntimeError: paddleocr is not installed, model init failed, or
                the installed API matches neither known shape.
        """
        if self._ocr is None:
            self._ocr = self._create()
        if hasattr(self._ocr, "predict"):
            return self._parse_v3(self._ocr.predict(image_bgr))
        if hasattr(self._ocr, "ocr"):
            return self._parse_v2(self._ocr.ocr(image_bgr))
        raise RuntimeError("model init failed: PaddleOCR has neither predict() nor ocr()")

    def _create(self) -> Any:
        try:
            from paddleocr import PaddleOCR
        except ImportError as exc:
            raise RuntimeError(f"paddleocr not installed: {exc}") from exc
        try:
            return PaddleOCR(lang=self.lang)
        except Exception as exc:
            raise RuntimeError(f"model init failed: {exc}") from exc

    @staticmethod
    def _poly_to_box(poly: Any) -> list[list[float]]:
        return [[float(x), float(y)] for x, y in np.asarray(poly).reshape(4, 2)]

    def _parse_v3(self, result: Any) -> list[Spot]:
        """Parse paddleocr >=3 predict output (list of dicts with rec_* / dt_polys)."""
        spots: list[Spot] = []
        for page in result:
            polys = np.asarray(page["dt_polys"])
            texts = list(page["rec_texts"])
            scores = [float(s) for s in page["rec_scores"]]
            for poly, text, score in zip(polys, texts, scores):
                spots.append(Spot(box=self._poly_to_box(poly), text=text, score=score))
        return spots

    def _parse_v2(self, result: Any) -> list[Spot]:
        """Parse paddleocr 2.x ocr() output ([[ [poly, (text, score)], ... ]] per image)."""
        spots: list[Spot] = []
        for page in result or []:
            for entry in page or []:
                poly, (text, score) = entry
                spots.append(Spot(box=self._poly_to_box(poly), text=text, score=float(score)))
        return spots
```

- [ ] **Step 5: Wire vào `__main__`**

`ReTran.OCR/src/retran_core/__main__.py` — thêm import và thay `_handlers`:

```python
from .ocr.handlers import make_spot_handler
from .ocr.paddle_engine import PaddleOcrEngine


def _handlers():
    return {
        "sidecar.ping": lambda p: "pong",
        "sidecar.version": lambda p: __version__,
        "ocr.spot": make_spot_handler(PaddleOcrEngine(lang="en")),
    }
```

- [ ] **Step 6: Chạy toàn bộ Python test — mong PASS (lần đầu integration sẽ tải model ~15-20MB)**

```powershell
# workdir: ReTran.OCR/
uv run pytest tests/ -v
# Expected: PASS (skip nếu paddleocr missing — nhưng Step 1 đã cài nên integration phải CHẠY THẬT)
```

- [ ] **Step 7: Smoke sidecar thật qua stdio (evidence cho report)**

```powershell
# workdir: ReTran.OCR/
$png = [Convert]::ToBase64String([IO.File]::ReadAllBytes((uv run python -c "import io,base64;from PIL import Image,ImageDraw;img=Image.new('RGB',(640,96),'white');ImageDraw.Draw(img).text((20,24),'HELLO RETRAN',fill='black');b=io.BytesIO();img.save(b,'PNG');import sys;open(sys.argv[1],'wb').write(b.getvalue())" "$env:TEMP\m2-smoke.png"); "$env:TEMP\m2-smoke.png"))
$p = Start-Process uv -ArgumentList "run","python","-m","retran_core","serve" -WorkingDirectory (Get-Location) -RedirectStandardInput "$env:TEMP\m2-in.txt" -RedirectStandardOutput "$env:TEMP\m2-out.txt" -PassThru -NoNewWindow
# (Cách đơn giản hơn cho evidence: chạy test integration Step 6 và paste output — PASSED với spots thật là đủ evidence.)
```

> Ghi chú cho implementer: evidence tối thiểu = output `uv run pytest tests/test_paddle_integration.py -v` PASSED. Không cần phiên stdio tay nếu test đã cover.

- [ ] **Step 8: Commit**

```powershell
git add "ReTran.OCR/"
git commit -m "feat(ocr): PaddleOcrEngine (lazy) + wire ocr.spot into sidecar serve"
```

---

### Task 3: `OcrDiff` (Core)

**Files:**
- Create: `ReTran.Core/Ocr/OcrDiff.cs`
- Create: `ReTran.Tests/OcrDiffTests.cs`

**Interfaces:**
- Consumes: —
- Produces (Task 4/6 dùng): `OcrDiff.Feed(IEnumerable<string> texts) -> IReadOnlyList<string>` — session-seen set, OrdinalIgnoreCase, bỏ whitespace, trả text mới theo thứ tự đầu vào.

- [ ] **Step 1: Viết failing test**

Create `ReTran.Tests/OcrDiffTests.cs`:

```csharp
using ReTran.Core.Ocr;
using Xunit;

namespace RetranCore.Tests;

public class OcrDiffTests
{
    [Fact]
    public void Feed_ReturnsNewTexts_ThenNothingForSame()
    {
        var diff = new OcrDiff();
        Assert.Equal(["HELLO", "WORLD"], diff.Feed(["HELLO", "WORLD"]));
        Assert.Empty(diff.Feed(["HELLO", "WORLD"]));
    }

    [Fact]
    public void Feed_IsCaseInsensitive()
    {
        var diff = new OcrDiff();
        Assert.Equal(["Hello"], diff.Feed(["Hello"]));
        Assert.Empty(diff.Feed(["HELLO"]));
    }

    [Fact]
    public void Feed_SkipsBlankTexts()
    {
        var diff = new OcrDiff();
        Assert.Equal(["A"], diff.Feed(["", "  ", "A"]));
        Assert.Empty(diff.Feed(["A", "  "]));
    }

    [Fact]
    public void Feed_PreservesInputOrder_AndNewOnly()
    {
        var diff = new OcrDiff();
        diff.Feed(["OLD"]);
        Assert.Equal(["N1", "N2"], diff.Feed(["OLD", "N1", "N2"]));
    }
}
```

- [ ] **Step 2: Chạy — mong FAIL**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter OcrDiffTests
# Expected: FAIL — compile error, namespace ReTran.Core.Ocr / OcrDiff chưa tồn tại
```

- [ ] **Step 3: Implement**

Create `ReTran.Core/Ocr/OcrDiff.cs`:

```csharp
namespace ReTran.Core.Ocr;

/// <summary>
/// Tracks texts seen during this session and returns only newly appearing ones.
/// Theo dõi text đã thấy trong phiên và chỉ trả về những text mới xuất hiện.
/// </summary>
public sealed class OcrDiff
{
    private readonly HashSet<string> _seen = new(StringComparer.OrdinalIgnoreCase);

    /// <summary>
    /// Feeds the current texts and returns those never seen before in this session.
    /// Chofeeds các text hiện tại và trả về những text chưa từng thấy trong phiên này.
    /// </summary>
    /// <param name="texts">Recognized texts from one OCR pass. Các text nhận được từ một lần OCR.</param>
    /// <returns>New non-blank texts in input order; all are marked seen. Các text mới không rỗng theo thứ tự đầu vào; tất cả được đánh dấu đã thấy.</returns>
    /// <exception cref="ArgumentNullException">Thrown when <paramref name="texts"/> is null.</exception>
    public IReadOnlyList<string> Feed(IEnumerable<string> texts)
    {
        ArgumentNullException.ThrowIfNull(texts);
        var fresh = new List<string>();
        foreach (var t in texts)
        {
            if (string.IsNullOrWhiteSpace(t)) continue;
            if (_seen.Add(t)) fresh.Add(t);
        }
        return fresh;
    }
}
```

- [ ] **Step 4: Chạy — mong PASS**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter OcrDiffTests
# Expected: PASS — 4 passed
```

- [ ] **Step 5: Commit**

```powershell
git add "ReTran.Core/Ocr/OcrDiff.cs" "ReTran.Tests/OcrDiffTests.cs"
git commit -m "feat(ocr): OcrDiff session-seen set for new-line detection"
```

---

### Task 4: `ISidecarRpc` + SidecarProcess error/EOF/stderr fixes + `OcrPipeline`

**Files:**
- Create: `ReTran.Core/OcrClient/ISidecarRpc.cs`
- Create: `ReTran.Core/OcrClient/SidecarRpcException.cs`
- Create: `ReTran.Core/Ocr/OcrPipeline.cs`
- Create: `ReTran.Tests/OcrPipelineTests.cs`
- Modify: `ReTran.Core/OcrClient/SidecarProcess.cs`
- Modify: `ReTran.Tests/SidecarProcessTests.cs`

**Interfaces:**
- Consumes: Task 1/2 (sidecar `ocr.spot` đã wire), Task 3 (`OcrDiff`), `JsonRpc`/`RpcErrorCodes` hiện hữu.
- Produces (Task 5/6 dùng):
  - `ISidecarRpc.CallAsync(string method, JsonNode? @params, CancellationToken ct) -> Task<JsonNode?>` — `SidecarProcess` implement sẵn
  - `SidecarRpcException(int Code, string Message)` — sidecar trả `error`
  - `OcrPipeline(ISidecarRpc rpc, OcrDiff diff)` + `Timeout` (init, default 30s) + `SpotAsync(string pngBase64, CancellationToken ct = default) -> Task<JsonObject>` — result = sidecar `{spots, elapsed_ms}` **+ `new_texts`**

- [ ] **Step 1: Viết failing tests**

Create `ReTran.Tests/OcrPipelineTests.cs`:

```csharp
using System.Text.Json.Nodes;
using ReTran.Core.Ocr;
using ReTran.Core.OcrClient;
using Xunit;

namespace RetranCore.Tests;

public class OcrPipelineTests
{
    private sealed class FakeSidecarRpc : ISidecarRpc
    {
        public JsonNode? NextResult { get; set; }
        public Exception? Throw { get; set; }
        public TimeSpan Delay { get; set; }
        public string? LastMethod { get; private set; }
        public JsonNode? LastParams { get; private set; }

        public async Task<JsonNode?> CallAsync(string method, JsonNode? @params, CancellationToken ct)
        {
            LastMethod = method;
            LastParams = @params;
            if (Delay > TimeSpan.Zero) await Task.Delay(Delay, ct);
            if (Throw is not null) throw Throw;
            return NextResult;
        }
    }

    private static JsonObject SpotResult(params string[] texts)
    {
        var spots = new JsonArray();
        foreach (var t in texts)
            spots.Add(new JsonObject
            {
                ["box"] = new JsonArray(
                    new JsonArray(0, 0), new JsonArray(1, 0),
                    new JsonArray(1, 1), new JsonArray(0, 1)),
                ["text"] = t,
                ["score"] = 0.9,
            });
        return new JsonObject { ["spots"] = spots, ["elapsed_ms"] = 12 };
    }

    [Fact]
    public async Task SpotAsync_PassesImageB64_AndAppendsNewTexts()
    {
        var fake = new FakeSidecarRpc { NextResult = SpotResult("HELLO", "WORLD") };
        var pipeline = new OcrPipeline(fake, new OcrDiff());
        var result = await pipeline.SpotAsync("BASE64PNG");
        Assert.Equal("ocr.spot", fake.LastMethod);
        Assert.Equal("BASE64PNG", fake.LastParams!["image"]!.GetValue<string>());
        Assert.Equal(12, result["elapsed_ms"]!.GetValue<int>());
        Assert.Equal(["HELLO", "WORLD"], result["new_texts"]!.AsArray().Select(n => n!.GetValue<string>()));
    }

    [Fact]
    public async Task SpotAsync_SecondCall_SameTexts_ReturnsEmptyNewTexts()
    {
        var fake = new FakeSidecarRpc { NextResult = SpotResult("HELLO") };
        var pipeline = new OcrPipeline(fake, new OcrDiff());
        await pipeline.SpotAsync("x");
        var second = await pipeline.SpotAsync("x");
        Assert.Empty(second["new_texts"]!.AsArray());
    }

    [Fact]
    public async Task SpotAsync_SidecarError_PropagatesAsSidecarRpcException()
    {
        var fake = new FakeSidecarRpc { Throw = new SidecarRpcException(-32602, "invalid PNG image: bad") };
        var pipeline = new OcrPipeline(fake, new OcrDiff());
        var ex = await Assert.ThrowsAsync<SidecarRpcException>(() => pipeline.SpotAsync("x"));
        Assert.Equal(-32602, ex.Code);
        Assert.Contains("invalid PNG", ex.Message);
    }

    [Fact]
    public async Task SpotAsync_Timeout_ThrowsTimeoutException()
    {
        var fake = new FakeSidecarRpc { Delay = TimeSpan.FromSeconds(5) };
        var pipeline = new OcrPipeline(fake, new OcrDiff()) { Timeout = TimeSpan.FromMilliseconds(200) };
        await Assert.ThrowsAsync<TimeoutException>(() => pipeline.SpotAsync("x"));
    }

    [Fact]
    public async Task SpotAsync_NoSpotsArray_ThrowsInvalidOperation()
    {
        var fake = new FakeSidecarRpc { NextResult = new JsonObject() };
        var pipeline = new OcrPipeline(fake, new OcrDiff());
        await Assert.ThrowsAsync<InvalidOperationException>(() => pipeline.SpotAsync("x"));
    }
}
```

Mở rộng `ReTran.Tests/SidecarProcessTests.cs` — thêm test (giữ nguyên test ping cũ):

```csharp
    [Fact]
    public async Task Spot_InvalidImage_SurfacesSidecarErrorAsException()
    {
        if (!OperatingSystem.IsWindows()) return;
        if (FindRepoRoot() is not string repoRoot) return;
        string pythonExe = Path.Combine(repoRoot, "ReTran.OCR", ".venv", "Scripts", "python.exe");
        if (!File.Exists(pythonExe)) return;
        await using var sp = await SidecarProcess.StartAsync(pythonExe, repoRoot, CancellationToken.None);
        var p = new JsonObject { ["image"] = "@@@" };
        var ex = await Assert.ThrowsAsync<SidecarRpcException>(() =>
            sp.CallAsync("ocr.spot", p, CancellationToken.None));
        Assert.Equal(-32602, ex.Code);
        Assert.Contains("invalid", ex.Message, StringComparison.OrdinalIgnoreCase);
    }
```

(cần `using System.Text.Json.Nodes;` — đã có sẵn trong file.)

- [ ] **Step 2: Chạy — mong FAIL**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter "OcrPipelineTests|SidecarProcessTests"
# Expected: FAIL — compile error (ISidecarRpc, OcrPipeline, SidecarRpcException chưa tồn tại)
```

- [ ] **Step 3: Implement**

`ReTran.Core/OcrClient/ISidecarRpc.cs`:

```csharp
using System.Text.Json.Nodes;

namespace ReTran.Core.OcrClient;

/// <summary>
/// JSON-RPC request surface over the sidecar stdio channel, mocked in pipeline tests.
/// Mặt phẳng yêu cầu JSON-RPC qua kênh stdio sidecar, được mock trong test pipeline.
/// </summary>
public interface ISidecarRpc
{
    /// <summary>
    /// Sends one request and awaits its response (matched by id).
    /// Gửi một yêu cầu và đợi phản hồi (ghép theo id).
    /// </summary>
    /// <exception cref="SidecarRpcException">Thrown when the sidecar replies with an error object. Khi sidecar trả về object lỗi.</exception>
    Task<JsonNode?> CallAsync(string method, JsonNode? @params, CancellationToken ct);
}
```

`ReTran.Core/OcrClient/SidecarRpcException.cs`:

```csharp
namespace ReTran.Core.OcrClient;

/// <summary>
/// Error reply from the sidecar process (JSON-RPC error code + message).
/// Phản hồi lỗi từ tiến trình sidecar (mã lỗi JSON-RPC + thông điệp).
/// </summary>
public sealed class SidecarRpcException(int code, string message) : Exception(message)
{
    /// <summary>JSON-RPC error code from the sidecar (-32602, -32603, ...). Mã lỗi JSON-RPC từ sidecar.</summary>
    public int Code { get; } = code;
}
```

`ReTran.Core/Ocr/OcrPipeline.cs`:

```csharp
using System.Diagnostics;
using System.Text.Json.Nodes;
using ReTran.Core.OcrClient;

namespace ReTran.Core.Ocr;

/// <summary>
/// Runs one OCR pass through the sidecar and appends Core's new-text diff to the result.
/// Chạy một lượt OCR qua sidecar và nối diff text mới của Core vào kết quả.
/// </summary>
public sealed class OcrPipeline
{
    private readonly ISidecarRpc _rpc;
    private readonly OcrDiff _diff;

    /// <summary>
    /// Per-call timeout including first-time model load (spec: 30s).
    /// Timeout mỗi lời gọi, tính cả lần nạp model đầu tiên (spec: 30s).
    /// </summary>
    public TimeSpan Timeout { get; init; } = TimeSpan.FromSeconds(30);

    /// <summary>Creates a pipeline over an RPC channel and a session diff tracker. Tạo pipeline trên kênh RPC và bộ diff theo phiên.</summary>
    public OcrPipeline(ISidecarRpc rpc, OcrDiff diff)
    {
        ArgumentNullException.ThrowIfNull(rpc);
        ArgumentNullException.ThrowIfNull(diff);
        _rpc = rpc;
        _diff = diff;
    }

    /// <summary>
    /// Calls sidecar ocr.spot with a base64 PNG and returns {spots, elapsed_ms, new_texts}.
    /// Gọi ocr.spot với PNG base64 và trả về {spots, elapsed_ms, new_texts}.
    /// </summary>
    /// <param name="pngBase64">Base64-encoded PNG without data-url prefix. PNG base64 không prefix data-url.</param>
    /// <param name="ct">Caller cancellation token. Token hủy của caller.</param>
    /// <exception cref="TimeoutException">Thrown when <see cref="Timeout"/> elapses. Khi hết <see cref="Timeout"/>.</exception>
    /// <exception cref="SidecarRpcException">Propagated from the sidecar. Được đẩy ra từ sidecar.</exception>
    /// <exception cref="InvalidOperationException">Thrown when the reply has no spots array. Khi phản hồi thiếu mảng spots.</exception>
    public async Task<JsonObject> SpotAsync(string pngBase64, CancellationToken ct = default)
    {
        ArgumentNullException.ThrowIfNull(pngBase64);
        using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
        cts.CancelAfter(Timeout);
        JsonNode? result;
        try
        {
            result = await _rpc.CallAsync("ocr.spot", new JsonObject { ["image"] = pngBase64 }, cts.Token);
        }
        catch (OperationCanceledException) when (!ct.IsCancellationRequested)
        {
            throw new TimeoutException($"ocr.spot timed out after {Timeout.TotalSeconds:0}s (includes first-time model load).");
        }

        if (result is not JsonObject obj || obj["spots"] is not JsonArray spotsArr)
            throw new InvalidOperationException("sidecar returned no 'spots' array.");

        var texts = new List<string>();
        foreach (var s in spotsArr)
            if (s is JsonObject so && so["text"] is JsonValue tv && tv.TryGetValue<string>(out var t))
                texts.Add(t);

        var newArr = new JsonArray();
        foreach (var n in _diff.Feed(texts)) newArr.Add(n);
        obj["new_texts"] = newArr;
        return obj;
    }
}
```

Sửa `SidecarProcess.cs` — 4 điểm:

**(a)** declaration + using:

```csharp
public sealed class SidecarProcess : IAsyncDisposable, ISidecarRpc
```

**(b)** trong `StartAsync`, sau `proc.Start();` thêm drain stderr (paddle viết rất nhiều stderr — không đọc sẽ đầy pipe và deadlock):

```csharp
        // Paddle floods stderr; an unread redirected pipe would fill up and deadlock the sidecar.
        proc.ErrorDataReceived += (_, e) => { if (e.Data is not null) Console.Error.WriteLine($"[sidecar] {e.Data}"); };
        proc.BeginErrorReadLine();
```

**(c)** thay vòng đọc kết quả trong `StartReadLoop`:

```csharp
                while ((line = await reader.ReadLineAsync()) is not null)
                {
                    if (Rpc.ParseLine(line) is not { } n
                        || n["id"]?.GetValue<string>() is not { } id)
                        continue;
                    if (n["result"] is { } r)
                    {
                        Deliver(id, r);
                    }
                    else if (n["error"] is { } err)
                    {
                        int code = err["code"] is JsonValue cv && cv.TryGetValue<int>(out var c)
                            ? c : RpcErrorCodes.InternalError;
                        string msg = err["message"] is JsonValue mv && mv.TryGetValue<string>(out var m)
                            ? m : "sidecar error";
                        Fail(id, new SidecarRpcException(code, msg));
                    }
                }
                FailAllPending(new SidecarRpcException(RpcErrorCodes.InternalError, "sidecar exited"));
```

**(d)** thêm 2 method bên dưới `Deliver`:

```csharp
    /// <summary>Routes a sidecar error reply to its waiter as an exception.</summary>
    private void Fail(string id, Exception ex)
    {
        if (_pending.TryGetValue(id, out var tcs)) { _pending.Remove(id); tcs.TrySetException(ex); }
    }

    /// <summary>Fails every waiter when the sidecar process exits.</summary>
    private void FailAllPending(Exception ex)
    {
        foreach (var tcs in _pending.Values) tcs.TrySetException(ex);
        _pending.Clear();
    }
```

(`Deliver` hiện có đã đúng — giữ nguyên. `_pending` truy cập từ read-loop và caller thread: dictionary không thread-safe; các thao tác đều diễn ra trên nhiều thread. Sửa tối thiểu an toàn: bọc `_pending` bằng `lock` trong `CallAsync` (đăng ký), `Deliver`, `Fail`, `FailAllPending`. Thêm `private readonly object _gate = new();` và lock trong 4 chỗ.)

- [ ] **Step 4: Chạy test — mong PASS**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter "OcrPipelineTests|SidecarProcessTests"
# Expected: PASS — 5 pipeline unit + 2 sidecar (ping, spot invalid image; latter skip nếu venv thiếu)
```

- [ ] **Step 5: Chạy full suite (đảm bảo không regression)**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0
# Expected: PASS — 42 cũ (38 + OcrDiffTests 4) + 6 mới (5 pipeline + 1 sidecar) = 48, 0 failed
```

- [ ] **Step 6: Commit**

```powershell
git add "ReTran.Core/OcrClient/ISidecarRpc.cs" "ReTran.Core/OcrClient/SidecarRpcException.cs" "ReTran.Core/Ocr/OcrPipeline.cs" "ReTran.Core/OcrClient/SidecarProcess.cs" "ReTran.Tests/OcrPipelineTests.cs" "ReTran.Tests/SidecarProcessTests.cs"
git commit -m "feat(ocr): OcrPipeline with 30s timeout; sidecar error/EOF routing + stderr drain"
```

---

### Task 5: CLI `ocr <input.png>`

**Files:**
- Create: `ReTran.Core/Ocr/OcrBootstrap.cs`
- Create: `ReTran.Core/Cli/OcrCommands.cs`
- Create: `ReTran.Tests/OcrCliTests.cs`
- Modify: `ReTran.Core/Program.cs` (đăng ký `ocr`, thay comment dòng 27)

**Interfaces:**
- Consumes: Task 4 (`OcrPipeline`, `SidecarProcess`, `ISidecarRpc`), `CoreConfig`/`CommandDispatcher`/`SidecarProcess.StartAsync` hiện hữu.
- Produces (Task 6 dùng): `OcrBootstrap.FindRepoRoot()` → `string?`; `OcrBootstrap.ResolvePythonExe(CoreConfig, string repoRoot)` → `string?`; `OcrCommands.Register(CommandDispatcher)`.
- Patterns tham chiếu: `FindRepoRoot` giống `SidecarProcessTests` (walk up tới khi có `ReTran.OCR/pyproject.toml`); python mặc định `<repoRoot>/ReTran.OCR/.venv/Scripts/python.exe`.

- [ ] **Step 1: Viết failing tests**

Create `ReTran.Tests/OcrCliTests.cs`:

```csharp
using System.Diagnostics;
using System.Text.Json.Nodes;
using ReTran.Core.Cli;
using ReTran.Core.Config;
using ReTran.Core.Ocr;
using Xunit;

namespace RetranCore.Tests;

public class OcrCliTests
{
    [Fact]
    public async Task Ocr_WithoutArgs_ReturnsUsageExit2()
    {
        var d = new CommandDispatcher();
        OcrCommands.Register(d);
        var (_, stderr, exit) = await RunDispatcherAsync(d, ["ocr"]);
        Assert.Equal(2, exit);
        Assert.Contains("usage", stderr, StringComparison.OrdinalIgnoreCase);
    }

    [Fact]
    public async Task Ocr_MissingFile_ReturnsExit1_BeforeAnySidecar()
    {
        var d = new CommandDispatcher();
        OcrCommands.Register(d);
        var (_, stderr, exit) = await RunDispatcherAsync(d,
            ["ocr", Path.Combine(Path.GetTempPath(), $"nope-{Guid.NewGuid():N}.png")]);
        Assert.Equal(1, exit);
        Assert.Contains("not found", stderr, StringComparison.OrdinalIgnoreCase);
    }

    [Fact]
    public void FindRepoRoot_FindsDirectoryContainingOcrProject()
    {
        string? root = OcrBootstrap.FindRepoRoot();
        if (root is null) return; // running outside repo layout -> skip
        Assert.True(File.Exists(Path.Combine(root, "ReTran.OCR", "pyproject.toml")));
    }

    [Fact]
    public async Task Ocr_RealSidecar_EndToEnd_PrintsSpotsJson()
    {
        // Infra gate: repo + venv + paddle import + model warm-up — otherwise skip (never fail on environment).
        if (!OperatingSystem.IsWindows()) return;
        string? repoRoot = OcrBootstrap.FindRepoRoot();
        if (repoRoot is null) return;
        string python = OcrBootstrap.ResolvePythonExe(new CoreConfig(), repoRoot) ?? "";
        if (python.Length == 0 || !File.Exists(python)) return;

        string fixture = CreateTextFixturePng();
        try
        {
            if (!await TryWarmUpModel(python, repoRoot, fixture)) return; // no paddle/model/network -> skip

            var d = new CommandDispatcher();
            OcrCommands.Register(d);
            var (stdout, stderr, exit) = await RunDispatcherAsync(d, ["ocr", fixture]);
            Assert.True(exit == 0, $"ocr CLI failed (exit {exit}): {stderr}");
            var json = JsonNode.Parse(stdout)!;
            Assert.NotEmpty(json["spots"]!.AsArray());
            Assert.Contains("HELLO", string.Join(" ", json["spots"]!.AsArray()
                .Select(s => s!["text"]!.GetValue<string>())).ToUpperInvariant());
            Assert.NotNull(json["new_texts"]);
        }
        finally { File.Delete(fixture); }
    }

    /// <summary>Runs one python snippet that loads PaddleOCR and OCRs the fixture (warms the model cache).</summary>
    private static async Task<bool> TryWarmUpModel(string python, string repoRoot, string fixturePng)
    {
        string script = Path.Combine(Path.GetTempPath(), $"retran-warmup-{Guid.NewGuid():N}.py");
        File.WriteAllText(script, """
            import sys
            import numpy as np
            from PIL import Image
            from paddleocr import PaddleOCR
            img = np.array(Image.open(sys.argv[1]).convert("RGB"))[:, :, ::-1]
            PaddleOCR(lang="en").predict(img)
            print("WARM_OK")
            """);
        try
        {
            var psi = new ProcessStartInfo
            {
                FileName = python,
                WorkingDirectory = repoRoot,
                RedirectStandardOutput = true,
                RedirectStandardError = true,
                CreateNoWindow = true,
            };
            psi.ArgumentList.Add(script);
            psi.ArgumentList.Add(fixturePng);
            using var p = Process.Start(psi)!;
            Task<string> outTask = p.StandardOutput.ReadToEndAsync();
            Task<string> errTask = p.StandardError.ReadToEndAsync();
            // generous: first run downloads the model (~15-20MB)
            bool exited = p.WaitForExit(300_000);
            if (!exited)
            {
                try { p.Kill(entireProcessTree: true); } catch { }
                return false;
            }
            _ = await errTask;
            string stdout = await outTask;
            return p.ExitCode == 0 && stdout.Contains("WARM_OK");
        }
        catch { return false; }
        finally { if (File.Exists(script)) File.Delete(script); }
    }

    /// <summary>Draws HELLO RETRAN on white; returns the temp PNG path (caller deletes).</summary>
    private static string CreateTextFixturePng()
    {
        string path = Path.Combine(Path.GetTempPath(), $"retran-ocr-fixture-{Guid.NewGuid():N}.png");
        using var bmp = new System.Drawing.Bitmap(640, 96);
        using var g = System.Drawing.Graphics.FromImage(bmp);
        g.Clear(System.Drawing.Color.White);
        using var font = new System.Drawing.Font("Arial", 48f, System.Drawing.FontStyle.Bold, System.Drawing.GraphicsUnit.Pixel);
        g.DrawString("HELLO RETRAN", font, System.Drawing.Brushes.Black, 10f, 20f);
        bmp.Save(path, System.Drawing.Imaging.ImageFormat.Png);
        return path;
    }

    private static async Task<(string Stdout, string Stderr, int Exit)> RunDispatcherAsync(
        CommandDispatcher d, string[] args)
    {
        var origOut = Console.Out;
        var origErr = Console.Error;
        using var so = new StringWriter();
        using var se = new StringWriter();
        Console.SetOut(so);
        Console.SetError(se);
        try
        {
            int exit = await d.DispatchAsync(args); // await BEFORE reading the writers
            return (so.ToString(), se.ToString(), exit);
        }
        finally { Console.SetOut(origOut); Console.SetError(origErr); }
    }
}
```

- [ ] **Step 2: Chạy — mong FAIL**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter OcrCliTests
# Expected: FAIL — compile error (OcrCommands/OcrBootstrap chưa tồn tại)
```

- [ ] **Step 3: Implement**

`ReTran.Core/Ocr/OcrBootstrap.cs`:

```csharp
using ReTran.Core.Config;

namespace ReTran.Core.Ocr;

/// <summary>
/// Resolves the repo root and Python interpreter shared by the ocr CLI and serve mode.
/// Tìm thư mục gốc repo và trình thông dịch Python dùng chung cho CLI ocr và serve.
/// </summary>
public static class OcrBootstrap
{
    /// <summary>
    /// Walks up from <paramref name="startDir"/> looking for ReTran.OCR/pyproject.toml.
    /// Đi ngược lên từ <paramref name="startDir"/> tìm ReTran.OCR/pyproject.toml.
    /// </summary>
    /// <returns>The repo root, or null when not inside the repo layout. Gốc repo, hoặc null khi không nằm trong layout repo.</returns>
    public static string? FindRepoRoot(string? startDir = null)
    {
        var d = new DirectoryInfo(startDir ?? Directory.GetCurrentDirectory());
        while (d is not null && !File.Exists(Path.Combine(d.FullName, "ReTran.OCR", "pyproject.toml")))
            d = d.Parent;
        return d?.FullName;
    }

    /// <summary>
    /// Resolves the sidecar Python: config PythonExe when it exists, else the uv venv under repoRoot.
    /// Tìm Python cho sidecar: config PythonExe khi tồn tại, không thì venv uv trong repoRoot.
    /// </summary>
    /// <returns>Full path to python.exe, or null when nothing usable exists. Đường dẫn python.exe đầy đủ, hoặc null khi không có.</returns>
    public static string? ResolvePythonExe(CoreConfig config, string repoRoot)
    {
        ArgumentNullException.ThrowIfNull(config);
        ArgumentNullException.ThrowIfNull(repoRoot);
        if (!string.IsNullOrWhiteSpace(config.PythonExe) && File.Exists(config.PythonExe))
            return config.PythonExe;
        string venv = Path.Combine(repoRoot, "ReTran.OCR", ".venv", "Scripts", "python.exe");
        return File.Exists(venv) ? venv : null;
    }
}
```

`ReTran.Core/Cli/OcrCommands.cs`:

```csharp
using System.Text.Json.Nodes;
using ReTran.Core.Config;
using ReTran.Core.Ocr;
using ReTran.Core.OcrClient;

namespace ReTran.Core.Cli;

/// <summary>
/// One-shot <c>ocr</c> CLI subcommand: PNG file in, spotted text JSON out.
/// Subcommand CLI <c>ocr</c> một lần: nhận tệp PNG, in JSON text đã nhận diện.
/// </summary>
public static class OcrCommands
{
    /// <summary>
    /// Registers the <c>ocr</c> command on <paramref name="d"/>.
    /// Đăng ký lệnh <c>ocr</c> vào <paramref name="d"/>.
    /// </summary>
    /// <param name="d">Dispatcher to register on. Dispatcher cần đăng ký.</param>
    /// <exception cref="ArgumentNullException">Thrown when <paramref name="d"/> is null.</exception>
    public static void Register(CommandDispatcher d)
    {
        ArgumentNullException.ThrowIfNull(d);
        d.Register("ocr", RunAsync);
    }

    private static async Task<int> RunAsync(string[] args)
    {
        if (args.Length != 1)
        {
            Console.Error.WriteLine("usage: ReTran.Core ocr <input.png>");
            return 2;
        }
        if (!File.Exists(args[0]))
        {
            Console.Error.WriteLine($"input not found: {args[0]}");
            return 1;
        }
        string? repoRoot = OcrBootstrap.FindRepoRoot();
        if (repoRoot is null)
        {
            Console.Error.WriteLine("repo root not found (ReTran.OCR/pyproject.toml above cwd).");
            return 1;
        }
        string? python = OcrBootstrap.ResolvePythonExe(CoreConfig.LoadFrom(CoreConfig.DefaultPath), repoRoot);
        if (python is null)
        {
            Console.Error.WriteLine("python not found (set PythonExe in config or run `uv sync` in ReTran.OCR/).");
            return 1;
        }
        try
        {
            await using var sp = await SidecarProcess.StartAsync(python, repoRoot, CancellationToken.None);
            var pipeline = new OcrPipeline(sp, new OcrDiff());
            string b64 = Convert.ToBase64String(await File.ReadAllBytesAsync(args[0]));
            JsonObject result = await pipeline.SpotAsync(b64);
            Console.Out.WriteLine(result.ToJsonString());
            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"ocr failed: {ex.Message}");
            return 1;
        }
    }
}
```

`ReTran.Core/Program.cs` — thay dòng comment `// ocr / translate handlers land in M2–M3.`:

```csharp
        OcrCommands.Register(dispatcher);
        // translate handler lands in M3.
```

(cần `using ReTran.Core.Cli;` — đã có sẵn.)

- [ ] **Step 4: Chạy unit (không cần model) — mong PASS**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter "OcrCliTests"
# Expected: unit 3 test PASS ngay; integration test PASS (warm-up + CLI) nếu đủ hạ tầng, skip- return nếu không
```

- [ ] **Step 5: Smoke CLI tay (evidence)**

```powershell
dotnet build "ReTran Project.slnx" --nologo -v q
# Tạo fixture như test (hoặc dùng capture): ReTran.Core\bin\Debug\net10.0-windows\ReTran.Core.exe capture screen --out $env:TEMP\m2-frame.png
ReTran.Core\bin\Debug\net10.0-windows\ReTran.Core.exe ocr $env:TEMP\m2-frame.png
# Expected: 1 dòng JSON có "spots" + "new_texts" (screenshot thật — text whatever xuất hiện trên màn hình), exit 0
```

Paste output (đã cắt bớt nếu dài) vào report.

- [ ] **Step 6: Chạy full suite**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0
# Expected: 48 + 4 = 52 passed, 0 failed (integration skip-returns nếu thiếu hạ tầng)
```

- [ ] **Step 7: Commit**

```powershell
git add "ReTran.Core/Ocr/OcrBootstrap.cs" "ReTran.Core/Cli/OcrCommands.cs" "ReTran.Core/Program.cs" "ReTran.Tests/OcrCliTests.cs"
git commit -m "feat(ocr): ReTran.Core ocr <png> CLI one-shot"
```

---

### Task 6: Serve mode — spawn sidecar + method `ocr.spot`

**Files:**
- Create: `ReTran.Core/JsonRpc/RpcMethodException.cs`
- Create: `ReTran.Tests/ServeOcrTests.cs`
- Modify: `ReTran.Core/JsonRpc/JsonRpcServer.cs` (catch `RpcMethodException` trước catch chung)
- Modify: `ReTran.Core/Program.cs` (`RunServeAsync`)

**Interfaces:**
- Consumes: Task 4 (`OcrPipeline`, `ISidecarRpc`), Task 5 (`OcrBootstrap`), `JsonRpcServer.Register`, `RpcErrorCodes`.
- Produces: Core serve method `ocr.spot` — params `{image}` → result `{spots, elapsed_ms, new_texts}`; `-32602` missing/empty/non-string image; `-32603` sidecar không sẵn sàng hoặc lỗi pipeline; `OcrBootstrap.TryStartSidecar(...)` (dùng lại từ CLI pattern).

- [ ] **Step 1: Viết failing test**

Create `ReTran.Tests/ServeOcrTests.cs`:

```csharp
using System.Text.Json.Nodes;
using ReTran.Core.JsonRpc;
using ReTran.Core.Ocr;
using ReTran.Core.OcrClient;
using Xunit;

namespace RetranCore.Tests;

public class ServeOcrTests
{
    private sealed class FakeTransport : IJsonRpcTransport
    {
        private readonly Queue<string> _in;
        public Queue<string> Out { get; } = new();
        public FakeTransport(IEnumerable<string> lines) { _in = new(lines); }
        public Task<string?> ReadLineAsync(CancellationToken ct) =>
            Task.FromResult(_in.Count > 0 ? _in.Dequeue() : null);
        public Task WriteLineAsync(string line, CancellationToken ct) { Out.Enqueue(line); return Task.CompletedTask; }
    }

    private sealed class FakeRpc : ISidecarRpc
    {
        public Task<JsonNode?> CallAsync(string method, JsonNode? @params, CancellationToken ct) =>
            Task.FromResult<JsonNode?>(new JsonObject
            {
                ["spots"] = new JsonArray(new JsonObject
                {
                    ["box"] = new JsonArray(new JsonArray(0, 0), new JsonArray(1, 0),
                        new JsonArray(1, 1), new JsonArray(0, 1)),
                    ["text"] = "HI",
                    ["score"] = 0.95,
                }),
                ["elapsed_ms"] = 3,
            });
    }

    private static async Task<JsonNode> CallOcrSpotOnce(string requestLine, OcrPipeline? pipeline)
    {
        var server = new JsonRpcServer();
        Program.RegisterOcrMethod(server, pipeline);
        var t = new FakeTransport([requestLine]);
        await server.RunAsync(t, CancellationToken.None);
        return JsonNode.Parse(t.Out.Dequeue())!;
    }

    [Fact]
    public async Task OcrSpot_MissingImage_ReturnsInvalidParams()
    {
        var resp = await CallOcrSpotOnce(
            """{"jsonrpc":"2.0","id":"1","method":"ocr.spot","params":{}}""",
            new OcrPipeline(new FakeRpc(), new OcrDiff()));
        Assert.Equal(RpcErrorCodes.InvalidParams, resp["error"]!["code"]!.GetValue<int>());
    }

    [Fact]
    public async Task OcrSpot_NoSidecar_ReturnsInternalError()
    {
        var resp = await CallOcrSpotOnce(
            """{"jsonrpc":"2.0","id":"2","method":"ocr.spot","params":{"image":"AAA"}}""",
            pipeline: null);
        Assert.Equal(RpcErrorCodes.InternalError, resp["error"]!["code"]!.GetValue<int>());
        Assert.Contains("sidecar", resp["error"]!["message"]!.GetValue<string>(), StringComparison.OrdinalIgnoreCase);
    }

    [Fact]
    public async Task OcrSpot_HappyPath_ReturnsSpotsAndNewTexts()
    {
        var pipeline = new OcrPipeline(new FakeRpc(), new OcrDiff());
        var resp = await CallOcrSpotOnce(
            """{"jsonrpc":"2.0","id":"3","method":"ocr.spot","params":{"image":"AAA"}}""", pipeline);
        Assert.Equal("HI", resp["result"]!["spots"]![0]!["text"]!.GetValue<string>());
        Assert.Equal("HI", resp["result"]!["new_texts"]![0]!.GetValue<string>());

        var second = await CallOcrSpotOnce(
            """{"jsonrpc":"2.0","id":"4","method":"ocr.spot","params":{"image":"BBB"}}""", pipeline);
        Assert.Empty(second["result"]!["new_texts"]!.AsArray());
    }
}
```

- [ ] **Step 2: Chạy — mong FAIL**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter ServeOcrTests
# Expected: FAIL — compile (Program.RegisterOcrMethod chưa tồn tại, RpcMethodException chưa tồn tại)
```

- [ ] **Step 3: Implement**

`ReTran.Core/JsonRpc/RpcMethodException.cs`:

```csharp
namespace ReTran.Core.JsonRpc;

/// <summary>
/// Handler exception that maps to a specific JSON-RPC error code instead of -32603.
/// Exception của handler được map sang mã lỗi JSON-RPC cụ thể thay vì -32603.
/// </summary>
public sealed class RpcMethodException(int code, string message) : Exception(message)
{
    /// <summary>JSON-RPC error code to return (-32602, -32603, ...). Mã lỗi JSON-RPC sẽ trả về.</summary>
    public int Code { get; } = code;
}
```

`JsonRpcServer.cs` — trong `try/catch` của `RunAsync`, thêm nhánh TRƯỚC `catch (Exception ex)`:

```csharp
            catch (RpcMethodException ex)
            {
                await transport.WriteLineAsync(JsonRpc.SerializeResponse(
                    new JsonRpcResponse(id, null, new JsonRpcError(ex.Code, ex.Message))), ct);
            }
            catch (Exception ex)
```

`ReTran.Core/Program.cs` — thêm method (public để test gọi, song ngữ doc) và sửa `RunServeAsync`:

```csharp
    /// <summary>
    /// Registers core method ocr.spot: validates {image} (-32602) and delegates to the pipeline (-32603 when unavailable).
    /// Đăng ký phương thức core ocr.spot: kiểm tra {image} (-32602) và ủy quyền cho pipeline (-32603 khi không sẵn sàng).
    /// </summary>
    /// <param name="server">Server to register on. Server cần đăng ký.</param>
    /// <param name="pipeline">Pipeline to sidecar, or null when the sidecar could not start. Pipeline tới sidecar, hoặc null khi chưa khởi tạo được.</param>
    /// <exception cref="ArgumentNullException">Thrown when <paramref name="server"/> is null.</exception>
    public static void RegisterOcrMethod(JsonRpcServer server, OcrPipeline? pipeline)
    {
        ArgumentNullException.ThrowIfNull(server);
        server.Register("ocr.spot", async p =>
        {
            if (pipeline is null)
                throw new RpcMethodException(RpcErrorCodes.InternalError,
                    "sidecar not available (python/venv missing or spawn failed).");
            if (p?["image"] is not JsonValue jv || !jv.TryGetValue<string>(out var image) || image.Length == 0)
                throw new RpcMethodException(RpcErrorCodes.InvalidParams,
                    "missing or empty 'image' (non-empty base64 PNG string).");
            return await pipeline.SpotAsync(image);
        });
    }
```

Thay `RunServeAsync`:

```csharp
    /// <summary>Runs the long-running JSON-RPC peer on stdio (spawns the OCR sidecar when possible).</summary>
    private static async Task<int> RunServeAsync()
    {
        var server = new JsonRpcServer();
        server.Register("core.ping", _ => Task.FromResult<JsonNode?>(JsonValue.Create("pong")));
        server.Register("core.version", _ => Task.FromResult<JsonNode?>(JsonValue.Create(Version)));

        ISidecarRpc? rpc = null;
        string? repoRoot = OcrBootstrap.FindRepoRoot();
        if (repoRoot is not null)
        {
            string? python = OcrBootstrap.ResolvePythonExe(Config.CoreConfig.LoadFrom(Config.CoreConfig.DefaultPath), repoRoot);
            if (python is not null)
            {
                try { rpc = await SidecarProcess.StartAsync(python, repoRoot, CancellationToken.None); }
                catch (Exception ex) { Console.Error.WriteLine($"sidecar spawn failed: {ex.Message}"); }
            }
        }
        RegisterOcrMethod(server, rpc is null ? null : new OcrPipeline(rpc, new OcrDiff()));

        var transport = new LineJsonRpcTransport(
            new StreamReader(Console.OpenStandardInput()),
            new StreamWriter(Console.OpenStandardOutput()) { NewLine = "\n" });
        await server.RunAsync(transport, CancellationToken.None);
        if (rpc is not null) await rpc.DisposeAsync();
        return 0;
    }
```

Cần thêm usings vào `Program.cs`: `using ReTran.Core.Ocr; using ReTran.Core.OcrClient;` (JsonRpc đã có).

- [ ] **Step 4: Chạy — mong PASS**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0 --filter ServeOcrTests
# Expected: PASS — 3 passed
```

- [ ] **Step 5: Smoke serve thật (evidence)**

```powershell
# Terminal A (workdir repo):
ReTran.Core\bin\Debug\net10.0-windows\ReTran.Core.exe --serve
# rồi gõ (mỗi dòng 1 request, Enter):
{"jsonrpc":"2.0","id":"1","method":"ocr.spot","params":{"image":"@@@"}}
# Expected: response {"error":{"code":-32602,...}}
{"jsonrpc":"2.0","id":"2","method":"ocr.spot","params":{}}
# Expected: {"error":{"code":-32602,...}}
# Ctrl+C để thoát. Paste 2 response vào report.
```

(Evidence tối thiểu: 2 response trên. Test unit đã cover happy path với fake.)

- [ ] **Step 6: Chạy full suite**

```powershell
dotnet test "ReTran.Tests/ReTran.Tests.csproj" -f net10.0-windows10.0.19041.0
# Expected: 52 + 3 = 55 passed, 0 failed
```

- [ ] **Step 7: Commit**

```powershell
git add "ReTran.Core/JsonRpc/RpcMethodException.cs" "ReTran.Core/JsonRpc/JsonRpcServer.cs" "ReTran.Core/Program.cs" "ReTran.Tests/ServeOcrTests.cs"
git commit -m "feat(ocr): serve-mode ocr.spot with sidecar spawn and precise error codes"
```

---

### Task 7: `Docs/performance.md` — đo thật

**Files:**
- Create: `Docs/performance.md`
- (scratch, KHÔNG commit): script đo đặt trong `%TEMP%` hoặc `.team-workspace/` (gitignored)

**Interfaces:**
- Consumes: sidecar `ocr.spot` đã chạy thật (Task 2), fixture 1920×1080.
- Produces: baseline số liệu M2 cho các milestone sau.

- [ ] **Step 1: Tạo fixture 1920×1080 có text**

```powershell
# workdir: ReTran.OCR/
uv run python -c @"
from PIL import Image, ImageDraw
img = Image.new('RGB', (1920, 1080), (20, 20, 30))
d = ImageDraw.Draw(img)
lines = ['The quick brown fox jumps over the lazy dog.', 'HELLO RETRAN - M2 OCR PERF TEST', 'Subtitle line number three for recognition load.']
for i, t in enumerate(lines):
    d.text((80, 200 + i * 160), t, fill=(240, 240, 240))
img.save(r'$env:TEMP\m2-perf-1080p.png'.replace('\$env', $env) if False else __import__('os').environ['TEMP'] + r'\m2-perf-1080p.png')
print('fixture written')
"@
# Expected: fixture written — (lưu ý: nếu PowerShell here-string escape gây lỗi, viết file .py trong %TEMP% rồi uv run python file.py)
```

- [ ] **Step 2: Đo — script driver (sidecar subprocess, timed 2 lần gọi, RSS)**

Viết `%TEMP%\m2-perf.py`:

```python
"""Measure sidecar OCR: cold-ish (first call incl. engine init) and warm latency + RSS."""
import json
import subprocess
import sys
import time
from pathlib import Path

import psutil  # via: uv run --with psutil

REPO = Path(sys.argv[1])
PNG_B64 = sys.argv[2]
PYTHON = str(REPO / "ReTran.OCR" / ".venv" / "Scripts" / "python.exe")

proc = subprocess.Popen(
    [PYTHON, "-m", "retran_core", "serve"],
    cwd=str(REPO / "ReTran.OCR"),
    stdin=subprocess.PIPE,
    stdout=subprocess.PIPE,
    stderr=subprocess.DEVNULL,
    text=True,
)

def rpc(method, params, id_):
    proc.stdin.write(json.dumps({"jsonrpc": "2.0", "id": str(id_), "method": method, "params": params}) + "\n")
    proc.stdin.flush()
    return json.loads(proc.stdout.readline())

def timed_spot(id_):
    t0 = time.perf_counter()
    resp = rpc("ocr.spot", {"image": PNG_B64}, id_)
    wall = time.perf_counter() - t0
    if "error" in resp:
        raise SystemExit(f"sidecar error: {resp['error']}")
    return wall, resp["result"]

t_start = time.perf_counter()
rpc("sidecar.ping", {}, 0)
t_serve = time.perf_counter() - t_start

ps = psutil.Process(proc.pid)
rss_before = ps.memory_info().rss
wall1, r1 = timed_spot(1)          # first call: engine init + maybe model load
rss_after = ps.memory_info().rss
wall2, r2 = timed_spot(2)          # warm
wall3, r3 = timed_spot(3)          # warm

proc.stdin.close()
proc.terminate()

print(json.dumps({
    "serve_ping_s": round(t_serve, 3),
    "cold_call_s": round(wall1, 3),
    "warm_call_s": [round(wall2, 3), round(wall3, 3)],
    "sidecar_elapsed_ms": [r1["elapsed_ms"], r2["elapsed_ms"], r3["elapsed_ms"]],
    "rss_before_mb": round(rss_before / 1048576, 1),
    "rss_after_mb": round(rss_after / 1048576, 1),
    "spots": len(r1["spots"]),
}, indent=2))
```

Chạy (base64 fixture):

```powershell
# workdir: ReTran.OCR/
$b64 = [Convert]::ToBase64String([IO.File]::ReadAllBytes("$env:TEMP\m2-perf-1080p.png"))
uv run --with psutil "$env:TEMP\m2-perf.py" (Get-Location).Path $b64
# Expected: JSON với cold_call_s, warm_call_s, rss_after_mb — PASTE số thật vào Step 3
```

- [ ] **Step 3: Ghi `Docs/performance.md` (số thật, không ước lượng)**

Create `Docs/performance.md`:

```markdown
# ReTran — Performance Budget Measurements

Mỗi milestone đo với game/thiết bị thật và ghi số vào đây.
Hard budget (spec §7): capture < 2% CPU; OCR ~3 fps, VRAM < 1GB; UI 10–15 fps; OBS ~10 fps; tổng < 8% CPU + < 500 MB RAM.

## M2 OCR baseline (2026-09-23)

Máy đo: <ghi CPU/GPU/RAM máy này — lấy từ Get-Counter / Get-CpuArchitecture hoặc `Get-CimInstance Win32_Processor`>.
Fixture: 1920×1080 PNG, 3 dòng text (script trong plan Task 7).
Backend: paddlepaddle 3.3.1 CPU (chưa dùng GPU — GPU là tối ưu milestone sau), PP-OCR mặc định, lang=en.
Phương pháp: script sidecar subprocess (`%TEMP%\m2-perf.py`), 1 warm-up ping + 3 lần `ocr.spot`, RSS qua psutil.

| Metric | Giá trị | Ghi chú |
|---|---|---|
| Serve + sidecar ping | <số>s | |
| Lần gọi đầu (cold: engine init [+ model load nếu chưa cache]) | <số>s | |
| Lần gọi warm 1 | <số>s | |
| Lần gọi warm 2 | <số>s | |
| RSS sidecar trước OCR | <số> MB | |
| RSS sidecar sau OCR | <số> MB | model resident |
| Số spots fixture | <số> | |

Đối chiếu budget: OCR budget ~3 fps → warm latency phải ≤ ~333ms để kịp 1 frame/lượt (throttle ở pipeline loop — chưa có trong M2).
Model det+rec en ~15–20MB tải lần đầu (cold call đầu tiên trên máy sạch sẽ cộng thêm thời gian download).

## M1 capture baseline

(chưa đo — ghi tại milestone có capture loop chạy thật)
```

- [ ] **Step 4: Verify file + commit**

```powershell
# Expected: file tồn tại, mọi ô <...> đã điền số thật / ghi chú rõ (không còn placeholder đo lường)
git add "Docs/performance.md"
git commit -m "docs(perf): M2 OCR baseline numbers (1080p, paddle CPU)"
```

---

## Self-Review (plan vs spec — đã chạy)

- **Spec coverage:** §1 scope ✅ (Tasks 1–7, không App/notification); §2 architecture ✅; §3.1 sidecar contract ✅ T1 (resolve: sidecar không trả `new_texts` — Core thêm ở T4/T5/T6); §3.2 OcrEngine ✅ T1/T2; §3.3 CLI ✅ T5 (timeout 30s qua Pipeline); §3.4 OcrDiff + 1 handler chung ✅ T3/T4/T5/T6; §4 error matrix ✅ (sidecar -32602/-32603 T1, Core serve -32602/-32603 T6, CLI exit 1 T5, timeout T4); §5 testing ✅ (unit + integration gated + skip êm); §5 performance.md ✅ T7; §6 ngoài-scope không bị làm; §7 rủi ro API paddle → T2 Step 2 probe + dual parser.
- **Resolve 2 điểm mơ hồ spec (đã ghi Global Constraints):** (1) sidecar vs Core `ocr.spot` result tách `new_texts`; (2) model CPU `paddlepaddle` (GPU chưa mandate trong M2 spec §7 — ghi chú trong performance.md).
- **Placeholder scan:** không TBD/TODO; mọi code step đầy đủ.
- **Type consistency:** `Spot.to_dict` ↔ `{box,text,score}`; `ISidecarRpc.CallAsync` khớp `SidecarProcess.CallAsync`; `OcrPipeline.SpotAsync → JsonObject` ↔ CLI/serve dùng chung; `OcrDiff.Feed → IReadOnlyList<string>` ↔ pipeline `new_texts`.
