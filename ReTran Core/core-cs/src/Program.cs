namespace ReTran.Core;

/// <summary>
/// Entry point placeholder — replaced by the CLI dispatcher / --serve loop in Task 3.
/// Chỗ giữ chỗ entry point — được thay bằng CLI dispatcher / vòng lặp --serve ở Task 3.
/// </summary>
public static class Program
{
    public static int Main(string[] args) => 0;
}
